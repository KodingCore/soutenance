<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Koding Core</title>
</head>
<body>
    <?php
    if(isset($_SESSION["user_id"])) //Si une session à été ouverte et la viriable "user_id" est défini
    {
        if(isset($_SESSION["role"])) //Si on lui à attribué un role
        {
            if($_SESSION["role"] === "admin") //Si il à le role d'aministrateur
            {
                require "views/roles-templates/admin/_header.phtml"; //On charge le header admin
            }
            else if($_SESSION["role"] === "user") //Si il à le role utilisateur
            {
                require "views/roles-templates/user/connected/_header.phtml"; //On charge le header user
            }
            
        }
        else //Si aucun role n'est défini
        {
            require "views/roles-templates/user/connected/_header.phtml"; //On charge le header user
        }
        
    }
    else //Si aucune session n'est ouverte
    {
        require "views/roles-templates/user/guest/_header.phtml"; //On require le header guest
    }

    require $this->template; // require du template en cours

    ?>
</body>
</html>