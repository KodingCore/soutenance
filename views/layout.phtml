<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KodingCore Development</title>
    <link rel="icon" type="image/png" href="https://i.goopics.net/wovpgb.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="assets/styles/css/style.css">
    <script src="https://kit.fontawesome.com/a2be3c71ec.js" crossorigin="anonymous"></script>
</head>
<body>
    <header id="top-header">
        <div id="header-container">
            <div class="logo">
                <img src="assets\images\logos\logo-kodingcore-white.png" alt="Logo KodingCore Development">
            </div>
            <div class="burger-container">
                <a href="#" id="burger-icon"><i class="fa-solid fa-bars fa-2x" style="color: #ffffff;"></i></a>
            </div>
            <?php
                if(isset($_SESSION["user_id"], $_SESSION["role"])) //* Control des variables de sessions pour l'acces users et admin
                {
                    if($_SESSION["role"] === "admin") //* Accès admin
                    {
                        require 'admin/_header.phtml';
                    }
                    else //* accès users
                    {
                        require 'user/_header.phtml';
                    }   
                }
                else //* accès visiteurs
                {
                    require 'guest/_header.phtml';
                }
            ?>
        </div>
    </header>
    <div id="left-aside-access">
        <i class="fa-solid fa-caret-right" style="color: #ffffff;"></i>
    </div>
        <?php
            require $this->template;
            if(isset($_SESSION["user_id"], $_SESSION["role"])) //* Control des variables de sessions pour l'acces users et admin
            {
                ?> <a href="index.php?route=request"><button id="float-btn">Votre site sur-mesure</button></a> <?php
            }
        ?>
    
    <footer id="top-footer">
        <?php
            
            if(isset($_SESSION["user_id"], $_SESSION["role"])) //* Control des variables de sessions pour l'acces users et admin
            {
                if($_SESSION["role"] === "admin") //* Accès admin
                {
                    require 'admin/_footer.phtml';
                }
                else //* accès users
                {
                    require 'user/_footer.phtml';
                }   
            }
            else //* accès visiteurs
            {
                require 'guest/_footer.phtml';
            }
        ?>
    </footer>
    <!--<section class="cookies">-->
    <!--    <div class="cookies-texte">-->
    <!--        <p>En pousuivant, vous acceptez l'utilisation des cookies par le -->
    <!--        site afin de vous proposer des contenus adaptés et réaliser des -->
    <!--        statistiques.-->
    <!--        </p>-->
    <!--    </div>-->
    <!--    <div class="cookies-btn">-->
    <!--        <button class="btn-success">-->
    <!--            Accepter les cookies-->
    <!--        </button>-->
    <!--        <button class="btn-deny">-->
    <!--            Refuser les cookies-->
    <!--        </button>-->
    <!--    </div>-->
    <!--</section>-->
</body>
<script type="module" src="assets/js/index.js"></script>
</html>