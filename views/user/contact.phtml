<main class="contact-container" id="contact-container">
    <header id="main-header">
        <div id="tagline-container">
            <p id="tagline">
                Besoin d'aide, d'un renseignement?
            </p>
        </div>
    </header>
    <h1>Envoyer un message</h1>
    <?php
    if(isset($this->data["message"]) && isset($this->data["field"])) //*Si on à un message dans la data
    {
        ?>
        <?php
        if($this->data["field"] === "general")
        {
            echo $this->data["message"];
        }
        ?>
        <?php
    }
    ?>
    <form action="index.php?route=contact" method="POST" id="contact_form">
        <fieldset>
            <label for="subject">Sujet</label>
            <input type="text" name="subject" id="subject" autocomplete="off" required minlength="4" maxlength="50">
            <?php
            if(isset($this->data["field"]) && $this->data["field"] === "subject")
            {
                ?>
                <span class="error"><?= $this->data["message"]; ?></span>
                <?php
            }?>
        </fieldset>
        <fieldset class="textarea">
            <label for="content">Message</label>
            <textarea id="content" name="content" placeholder="Votre texte ici..." required minlength="10" maxlength="2048"></textarea>
            <?php
            if(isset($this->data["field"]) && $this->data["field"] === "content")
            {
                ?>
                <span class="error"><?= $this->data["message"]; ?></span>
                <?php
            }?>
        </fieldset>
        <input type="submit" value="Envoyer">
    </form>
</main>